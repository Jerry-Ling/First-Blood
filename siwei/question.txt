1.用一维数组存点的话
a[a*b*c*d];
A[a1][b1][c1][d1]=a[a1*(b*c*d)+b1*(c*d)+c1*d+d1];
多维数组转化为一维数组存储。
2.
解决办法：
首先，使用malloc 函数给四维数组分配空间，接下来使用指针来做需要做的变换，
具体的变化，//新建一个空的四维数组（大小与第一个数组的大小相同）/\/
将数组的每个维的地址变换位置完成转换？
 	
将每个维度的地址按需要做转换反回转换后的数组；
在主函数中定义数组大小，并赋值（使用0-100的随机数）接下来调用函数将转换来的数组与之前的数组作比较，实验数据使用定义范围内数字的随机数，如果成立，输出成立，否则输出失败。


1.自己写的算法不对，结果不是自己想要的输出
2.大概知道你想要我怎么去做这个问题，但是网上找的资料不理解怎么回事。
想我做的:将四维数组转化维一维数组，使用malloc函数给数组分配空间使用每个维度的
第一个内存位置确定每个维度一共的长度，然后将这些内存地址交换，完成转置。
接下来在主函数中按条件使用if语句判断是否正确。
主要是讲一下指针在内存里的用法
